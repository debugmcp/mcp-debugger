**The npm package is a pi coding-agent package and ships the agent skill** — `@debugmcp/mcp-debugger` now carries `skills/debugging/` and a `pi` manifest (the skill directory plus a `pi-mcp-adapter` server entry in `pi.mcp.json`), so `pi install npm:@debugmcp/mcp-debugger` registers the server and the skill with no clone; the skill is also reachable at `node_modules/@debugmcp/mcp-debugger/skills/debugging` for any harness that can point at a skills directory (#714)
