**Install the repo as a pi coding-agent package** — a `pi` manifest in the root `package.json` (plus `pi.mcp.json` and a thin `pi-extension/`) registers the stdio server with pi-mcp-adapter, discovers the existing `skills/debugging/` skill, and adds a session-start hint, so `pi install <path>` wires up all 28 debugger tools with no manual config (#714)
