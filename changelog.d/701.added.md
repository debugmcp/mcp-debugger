**`start_debugging` says when the program ran to completion** — a launch that ended before any user-visible stop answered a bare `state: "stopped"`, and nothing in the response said the debuggee had exited, with what code, or that its breakpoints — verified ones included — were never hit; the #467 warning only names breakpoints that never bound. The response now says the program ran to completion, carries `data.exitCode` when the debuggee reported one, lists every line breakpoint it ran past as `data.unhitBreakpoints` (`file`, `line`, `verified`), and, when a verified breakpoint went unhit, points at `dapLaunchArgs: { stopOnEntry: true }` for a line that runs while its module loads (#701)
