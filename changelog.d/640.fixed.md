**`npm run test:docker-e2e` runs again** — the script pointed at `tests/e2e/docker/docker-proxy-liveness.test.ts`, deleted in October 2025, so it failed on a missing file instead of testing anything; it also set a `DOCKER_E2E` env var no test reads. It now runs the whole `tests/e2e/docker/` suite; the smoke tests build their own `mcp-debugger:test` image when it is missing or stale, so re-runs against a fresh image skip the build — unlike `test:e2e:container`, which always rebuilds `mcp-debugger:local` from scratch with `--no-cache` first. (#640)
