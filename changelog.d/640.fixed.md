**`npm run test:docker-e2e` runs again** — the script pointed at `tests/e2e/docker/docker-proxy-liveness.test.ts`, deleted in October 2025, so it failed on a missing file instead of testing anything; it also set a `DOCKER_E2E` env var no test reads. It now runs the whole `tests/e2e/docker/` suite against an existing `mcp-debugger:local` image — the build-free companion to `test:e2e:container`, which still rebuilds the image first. (#640)
