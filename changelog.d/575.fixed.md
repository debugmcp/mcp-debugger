**Session failures are now classified by typed errors instead of message text** — user-controlled paths, expressions, and breakpoint text containing words such as `closed`, `terminated`, or `not found` can no longer change an MCP error into a `{success:false}` result (#575)
