**JavaScript attach waits for a usable target even without an entry pause.** Both attach modes honor thread verification and its timeout; child threads become available after adoption and configuration finish. Parent start intent stays separate from adapter arguments, and launch breakpoint re-sync requests a fresh adapter echo. Attach preserves breakpoint stops and termination observed during initialization (#758, #762, #730, #705).
