**Dev-proxy stays responsive during rebuilds** — builds run asynchronously while the current
backend continues serving requests. Build-and-restart operations preserve each request's environment
and response state, and timeout or shutdown terminates owned build descendants and cancels queued
restarts. Status reports `buildInProgress` (#748, #756, #757).
