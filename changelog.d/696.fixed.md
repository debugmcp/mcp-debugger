**Every adapter package's tests run standalone** — `pnpm --filter @debugmcp/adapter-ruby test` (and the same for go, cpp, dotnet and codelldb-common) failed at setup before a single test ran: those packages had no `vitest.config.ts`, so vitest walked up to the root config, whose `setupFiles` entry resolves against the package's own cwd and points at a file that does not exist there. Each of the five now ships the same per-package config rust, mock, python and javascript already had; the config also aliases `@debugmcp/codelldb-common` to source for the two packages that import it (rust, cpp), so a fresh checkout with no `dist/` resolves it the same way it resolves `@debugmcp/shared`. Two tests that relied on the root setup's per-test `vi.unstubAllEnvs()` now clean up their own env stubs, and the adapter development guide tells a new adapter to ship the file (#696)
