`attach_to_process` now returns `state: "running", pending: true` when a
requested post-attach pause has not produced a stopped event yet. Only the
late event transitions the session to `paused`, and every paused session has
a recorded `lastStop`.
