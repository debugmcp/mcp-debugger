**`doctor` now sees `DLV_PATH`** — the Delve probe behind the doctor table ignored the `DLV_PATH` environment variable, so on a machine where `dlv` is not on PATH doctor reported Go's backend as missing while a real session (which honors the variable) worked fine. `findDelveExecutable` now reads `DLV_PATH` itself with the same use-if-it-exists-else-keep-looking semantics a session gets, so doctor and sessions agree by construction. (#639)
