**Inspection fallbacks find useful locals and explain frameless threads** — JavaScript local inspection now tries Closure, Module/Script, and Global scopes before leaving the selected frame; Ruby ignores rdbg `%` pseudo-variables unless special variables are requested; and `get_stack_trace` identifies the inspected thread and suggests an alternative when an explicitly requested thread has no frames (#548, #549, #553)
