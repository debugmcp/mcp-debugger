**The HTTP 403 for a rejected `Host` names the cause and the remedy, and the Docker image `EXPOSE`s the port the CLI actually defaults to** — `Invalid Host: mcp-debugger` read like a malformed request; the body now says which Host values are accepted, that the check is DNS-rebinding protection, and that the fix is a port-forward/SSH tunnel or an explicit `--allowed-host` / `MCP_HTTP_ALLOWED_HOSTS` opt-in, and each distinct rejected hostname is logged once on the server. The image declared `EXPOSE 3000` while `http` defaults to 3001, so `docker run -p 3000:3000 ... http` silently published the wrong port; it now declares 3001. The README, Docker, Kubernetes, and troubleshooting docs gain the loopback-Host note that would have made the bundle archaeology unnecessary (#667)
