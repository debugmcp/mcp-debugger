**`start_debugging` now reports why the adapter refused the launch instead of "Proxy exited during initialization"** — a Delve too old for the Go toolchain answered its DAP `launch` with the reason ("Version of Delve is too old for Go version go1.27.1 …"), but the tool result said only `Proxy exited during initialization. Code: 0` followed by proxy stderr. Two drops: the response's user-facing `body.error.format` lost to its short `message` ("Failed to launch"), and the proxy worker sent its init-failure report only after its shutdown had waited on the debuggee and swept the adapter's process group, by which time the adapter's own exit had already failed the launch generically. The report now goes out before the teardown and carries the formatted DAP reason (#736)
