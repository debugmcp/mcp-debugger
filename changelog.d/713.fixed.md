**`adapter-configured` (with `init-received` and `dry-run-complete`) fired every listener twice per status message** — every worker status ran through both ProxyManager's imperative handler and the functional core, and both emitted the event; only idempotent listeners kept it harmless, and since #704 the JavaScript launch takes this path on every child adoption. Status-derived events are now emitted once, by the imperative handler that owns their latches (#713)
