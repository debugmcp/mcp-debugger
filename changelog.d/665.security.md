**`fast-uri` floor raised to 3.1.6** — the `pnpm.overrides` pin for the MCP SDK's transitive `ajv` → `fast-uri` dependency rises from `^3.1.4` to `^3.1.6` (resolving 3.1.7), clearing the four high-severity SSRF / host-confusion advisories published against `fast-uri` < 3.1.6 that were failing `pnpm audit --prod --audit-level=high` on every PR (#665)
