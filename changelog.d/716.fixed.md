**Self-debugging starts with a complete tool inventory and a working nested server** — the
development proxy waits up to 30 seconds for initial backend startup before listing its tools,
retaining recovery tools on failure or timeout. The bundled CLI scopes its internal startup
suppression flag to the core import and restores the caller's environment before launching
adapters or debuggees (#716, #717).
