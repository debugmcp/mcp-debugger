**`remove_breakpoint {function}` now removes every matching function breakpoint in one round trip** — a name that matched N records was removed one record at a time, and because DAP `setFunctionBreakpoints` is replace-all for the whole session, each of those removals re-sent the entire surviving set: N round trips to the adapter, any live-sync failure reported N times in one joined `warning`, and a window between the sends in which the not-yet-deleted duplicates were still armed in the debuggee. All matches are now deleted first and the surviving set is re-sent once. Function-breakpoint name resolution — the policy-certain rewrite (#467) and the per-adapter name advisory (#303/#308) — moved into the session layer with it, so `set_breakpoint` and `remove_breakpoint` address the same name by construction. Tool response shapes are unchanged (#559)
