# packages/adapter-rust/vendor/codelldb/darwin-x64/lldb/lib/python3.12/site-packages/pip/_vendor/urllib3/util/__init__.py
@source-hash: 2449929a6aaa2f26
@generated: 2026-02-09T17:57:37Z

**Purpose**: Main public interface module for urllib3.util package that aggregates and re-exports utility functions from various submodules for backwards compatibility.

**Key Imports and Re-exports**:

**Connection utilities** (L4):
- `is_connection_dropped` - Connection state validation

**Request utilities** (L5):
- `SKIP_HEADER`, `SKIPPABLE_HEADERS` - Header filtering constants
- `make_headers` - HTTP header construction helper

**Response utilities** (L6):
- `is_fp_closed` - File pointer state checking

**Retry logic** (L7):
- `Retry` - Request retry mechanism class

**SSL/TLS utilities** (L8-19):
- SSL constants: `ALPN_PROTOCOLS`, `HAS_SNI`, `IS_PYOPENSSL`, `IS_SECURETRANSPORT`, `PROTOCOL_TLS`
- `SSLContext` - SSL context management
- SSL functions: `assert_fingerprint`, `resolve_cert_reqs`, `resolve_ssl_version`, `ssl_wrap_socket`

**Timeout utilities** (L20):
- `Timeout` - Timeout configuration class
- `current_time` - Time utility function

**URL utilities** (L21):
- `Url` - URL data structure
- URL functions: `get_host`, `parse_url`, `split_first`

**I/O utilities** (L22):
- `wait_for_read`, `wait_for_write` - Async I/O helpers

**Public API** (L24-49):
- `__all__` tuple explicitly defines the complete public interface, ensuring controlled exports for package consumers

**Architecture**: Classic aggregator pattern that centralizes imports from specialized submodules, providing a clean single-import interface while maintaining internal modularity. All functionality is delegated to respective submodules.