# packages/adapter-rust/vendor/codelldb/darwin-x64/lldb/lib/python3.12/site-packages/pip/_vendor/pyproject_hooks/__init__.py
@source-hash: 9027a19b2d146816
@generated: 2026-02-09T17:59:48Z

**Purpose**: Entry point module for pyproject_hooks package, providing a clean API for calling pyproject.toml-based Python build backend hooks. Acts as a public interface wrapper around internal implementation.

**Key Exports**:
- Exception classes for build backend error handling:
  - `BackendUnavailable` (L6, L16): Raised when build backend cannot be loaded
  - `BackendInvalid` (L5, L17): Raised when build backend is malformed or invalid
  - `HookMissing` (L8, L18): Raised when required build hook is not implemented
  - `UnsupportedOperation` (L9, L19): Raised for unsupported build operations

- Core functionality:
  - `BuildBackendHookCaller` (L7, L22): Main class for invoking build backend hooks
  - `default_subprocess_runner` (L10, L20): Standard subprocess execution utility
  - `quiet_subprocess_runner` (L11, L21): Silent subprocess execution utility

**Architecture**: 
- Re-export pattern from `._impl` module (L4-12) to maintain clean public API
- All implementation details isolated in private `_impl` module
- Version defined as string constant (L14)
- Explicit `__all__` declaration (L15-23) controls public API surface

**Dependencies**: 
- Internal `._impl` module contains actual implementations
- Standard library only (no external dependencies at this level)

**Usage Context**: 
This module serves as the primary import point for tools needing to interact with PEP 517/518 build backends, providing standardized error handling and subprocess utilities for build operations.