# packages/adapter-rust/vendor/codelldb/darwin-x64/lldb/lib/python3.12/site-packages/pip/_vendor/urllib3/contrib/_securetransport/__init__.py
@source-hash: e3b0c44298fc1c14
@generated: 2026-02-09T17:56:48Z

## Empty Python Package Init File

This is an empty `__init__.py` file for the `_securetransport` module within the urllib3 contrib package. The file serves as a package marker to make Python treat the directory as a package/module.

**Purpose:**
- Enables import of the `_securetransport` package
- Likely contains or will contain macOS SecureTransport backend implementations for urllib3
- Part of the urllib3 contributed adapters for platform-specific SSL/TLS implementations

**Structure:**
- No classes, functions, or variables defined
- No imports or dependencies
- Completely empty implementation

**Context:**
- Located within pip's vendored urllib3 copy
- Part of the macOS-specific SecureTransport integration path
- The actual SecureTransport implementation would be in sibling modules within this package

**Usage Pattern:**
This empty init file allows other modules to import from `urllib3.contrib._securetransport` namespace, with the actual functionality implemented in separate modules within the same package directory.