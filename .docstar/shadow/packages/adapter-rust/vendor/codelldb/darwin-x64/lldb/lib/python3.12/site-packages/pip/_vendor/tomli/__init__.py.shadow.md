# packages/adapter-rust/vendor/codelldb/darwin-x64/lldb/lib/python3.12/site-packages/pip/_vendor/tomli/__init__.py
@source-hash: 26153057ae830758
@generated: 2026-02-09T18:00:05Z

**Primary Purpose**: Package initialization module for tomli, a TOML parser library. Provides public API interface by re-exporting core parsing functions and exceptions from internal modules.

**Key Components**:
- `__all__` (L5): Defines public API exports - `loads`, `load`, and `TOMLDecodeError`
- `__version__` (L6): Version string "2.0.1" with automated versioning constraint
- Public API imports (L8): Re-exports `TOMLDecodeError`, `load`, `loads` from `._parser` module
- Exception module override (L11): Modifies `TOMLDecodeError.__module__` to appear as if defined in this package root

**Dependencies**:
- Internal `._parser` module containing actual implementation

**Architectural Pattern**: 
Standard Python package facade pattern - internal implementation hidden behind clean public interface. Exception module manipulation ensures consistent public API presentation.

**Key Constraints**:
- Version string must be managed by bump2version utility (comment on L6)
- Exception appears to originate from package root rather than internal module for cleaner tracebacks