# packages/adapter-rust/vendor/codelldb/darwin-arm64/lldb/lib/python3.12/site-packages/pip/_vendor/rich/color_triplet.py
@source-hash: de585091d25bbd63
@generated: 2026-02-09T17:59:01Z

**Purpose:** A simple data structure representing RGB color values with utility methods for format conversion.

**Core Class:**
- `ColorTriplet` (L4-38): NamedTuple-based immutable RGB color container with three integer components (red, green, blue) in 0-255 range

**Key Components:**
- `red` (L7): Red color component field with type annotation and documentation
- `green` (L9): Green color component field with type annotation and documentation  
- `blue` (L11): Blue color component field with type annotation and documentation

**Conversion Methods:**
- `hex` property (L14-18): Converts RGB values to CSS-style hex format (#rrggbb) using lowercase hexadecimal
- `rgb` property (L20-28): Formats as CSS rgb() function string representation
- `normalized` property (L30-38): Converts 0-255 integer values to 0.0-1.0 float range for mathematical operations

**Dependencies:**
- `typing.NamedTuple`: Provides immutable tuple-like base class with named fields
- `typing.Tuple`: Type annotation for normalized property return value

**Design Patterns:**
- Immutable data class pattern via NamedTuple inheritance
- Property-based API for different format representations
- Type-safe field definitions with explicit ranges documented

**Key Invariants:**
- RGB components must be integers in 0-255 range (enforced by documentation contract)
- Immutability guaranteed by NamedTuple base class
- Consistent unpacking pattern used across all methods (red, green, blue = self)