# packages/adapter-rust/vendor/codelldb/darwin-arm64/lldb/lib/python3.12/site-packages/pip/_vendor/rich/screen.py
@source-hash: 628791784494871e
@generated: 2026-02-09T17:59:13Z

**Primary Purpose**: Rich terminal library's Screen class - a renderable that fills the entire terminal screen and crops content that exceeds screen dimensions.

**Key Components**:
- `Screen` class (L18-54): Main renderable wrapper that manages full-screen display
  - `__init__` (L28-38): Accepts multiple renderables, wraps them in a Group, configures background style and application mode
  - `__rich_console__` (L40-54): Core rendering method that sizes content to terminal dimensions and handles line endings

**Key Dependencies**:
- `.segment.Segment`: Text segment handling with styling
- `.style.StyleType`: Style type definitions
- `._loop.loop_last`: Utility for iterating with last-item detection
- `pip._vendor.rich.console.Group`: Container for multiple renderables (imported at runtime L34)

**Architectural Patterns**:
- Implements Rich's renderable protocol via `__rich_console__` method
- Uses composition pattern - wraps child renderables in a Group container
- Deferred import pattern for Group to avoid circular dependencies

**Key Behaviors**:
- Automatically sizes content to terminal dimensions using `options.size` (L43)
- Applies background styling across entire screen area (L44, L49)
- Handles padding to fill screen space via `pad=True` (L47)
- Uses `Segment.set_shape()` for precise dimension control (L49)
- Switches line ending style based on application_mode: `\n\r` vs standard line segments (L50)

**Critical Constraints**:
- Content exceeding screen dimensions gets cropped
- Background style applies to entire screen area when specified
- Application mode affects line ending behavior for terminal compatibility