# packages/adapter-rust/vendor/codelldb/linux-arm64/lldb/lib/python3.12/site-packages/pip/_vendor/pyproject_hooks/__init__.py
@source-hash: 9027a19b2d146816
@generated: 2026-02-09T18:00:39Z

**Primary Purpose**: Public API module for pyproject_hooks package, providing wrappers to call pyproject.toml-based build backend hooks. Acts as the main entry point for consumers of the library.

**Key Components**:
- **Module docstring (L1-2)**: Describes the package's core functionality for calling pyproject.toml build backend hooks
- **Version declaration (L14)**: Package version '1.0.0' 
- **Public API exports (L15-23)**: Defines `__all__` list containing all publicly available components

**Imported Components from _impl module (L4-12)**:
- **Exception classes**: `BackendInvalid`, `BackendUnavailable`, `HookMissing`, `UnsupportedOperation` - for error handling in build backend operations
- **Core class**: `BuildBackendHookCaller` - main class for invoking build backend hooks
- **Utility functions**: `default_subprocess_runner`, `quiet_subprocess_runner` - subprocess execution utilities for backend operations

**Architecture Pattern**: 
- Classic Python package structure using `__init__.py` as public API facade
- Implementation details hidden in private `_impl` module
- Clean separation between public interface and internal implementation
- All imported components are re-exported via `__all__` for controlled public API

**Dependencies**: 
- Internal dependency on `._impl` module containing actual implementation
- No external dependencies visible at this level

**Usage Context**: 
This module serves as the primary interface for tools that need to interact with Python package build backends defined in pyproject.toml files, following PEP 517/518 standards.