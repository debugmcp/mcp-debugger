# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/freebsd13/x86_64.rs
@source-hash: 64c4bd82eaf30a26
@generated: 2026-02-09T17:56:49Z

## Purpose
Platform-specific system constants for FreeBSD 13 on x86_64 architecture, providing KPTI (Kernel Page Table Isolation) control and status definitions for process control operations.

## Key Elements
- **PROC_KPTI_CTL (L3)**: Base constant for KPTI control operations, derived from `PROC_PROCCTL_MD_MIN`
- **PROC_KPTI_CTL_ENABLE_ON_EXEC (L4)**: Flag to enable KPTI on process execution (value: 1)
- **PROC_KPTI_CTL_DISABLE_ON_EXEC (L5)**: Flag to disable KPTI on process execution (value: 2)
- **PROC_KPTI_STATUS (L6)**: Constant for querying KPTI status, offset by 1 from the base control constant
- **PROC_KPTI_STATUS_ACTIVE (L7)**: Bitmask indicating active KPTI state (0x80000000)

## Dependencies
- Uses `crate::prelude::*` for common type imports (L1)
- References `crate::PROC_PROCCTL_MD_MIN` as base offset for machine-dependent process control constants

## Architectural Context
Part of libc's hierarchical platform abstraction: unix → bsd → freebsdlike → freebsd → freebsd13 → x86_64. These constants enable userspace programs to interact with FreeBSD's KPTI security feature through the procctl system call interface.

## Usage Pattern
Constants follow FreeBSD's procctl API conventions where control operations use sequential offsets from a base value, and status queries use bitmask flags for state representation.