# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/hurd/b64.rs
@source-hash: b9b2082e721a5ec8
@generated: 2026-02-09T18:03:16Z

## Purpose
Platform-specific type definitions for GNU Hurd operating system on 64-bit architecture. Provides C-compatible type mappings and ELF (Executable and Linkable Format) structures for system-level programming and binary analysis.

## Key Type Categories

### Integer Type Mappings (L3-26)
- **64-bit base types**: `__int64_t`, `__uint64_t` (L3-4) - fundamental 64-bit signed/unsigned integers
- **Fast integer types**: `int_fast16_t`, `int_fast32_t`, `int_fast64_t` and unsigned variants (L6-11) - optimized for speed, all map to `c_long`/`c_ulong`
- **Quad types**: `__quad_t`, `__u_quad_t` (L13-14) - quadword (64-bit) integers
- **Max types**: `__intmax_t`, `__uintmax_t` (L15-16) - largest supported integer types
- **System word types**: Various internal system types like `__squad_type`, `__sword_type` (L18-25)
- **IPC type**: `__ipc_pid_t` (L27) - inter-process communication process ID

### ELF Binary Format Types (L29-43)
- **Primitive ELF types**: `Elf64_Half` (u16), `Elf64_Word` (u32), `Elf64_Off` (u64), etc. (L29-35)
- **Generic ELF aliases**: `Elf_Addr`, `Elf_Half`, etc. (L37-42) - platform-agnostic references to 64-bit variants

### ELF Structure Definitions (L44-94)
- **`Elf64_Ehdr` (L45-60)**: ELF header structure containing file metadata (entry point, section info, etc.)
- **`Elf64_Shdr` (L62-73)**: Section header describing memory sections in ELF files
- **`Elf64_Sym` (L75-82)**: Symbol table entry for linking and debugging
- **`Elf64_Phdr` (L84-93)**: Program header describing memory segments for loading

## Dependencies
- Uses `crate::prelude::*` (L1) for common libc types (`c_long`, `c_ulong`, `c_int`, etc.)
- Employs `s!` macro (L44) for structure definitions with C-compatible layout

## Architectural Notes
- All "fast" integer types map to native word size (`c_long`/`c_ulong`) for optimal performance on 64-bit Hurd
- ELF structures follow standard ELF64 specification for binary compatibility
- Type aliases provide both specific (`Elf64_*`) and generic (`Elf_*`) interfaces