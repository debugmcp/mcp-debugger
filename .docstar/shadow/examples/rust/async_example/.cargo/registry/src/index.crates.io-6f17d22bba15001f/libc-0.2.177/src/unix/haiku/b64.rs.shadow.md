# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/haiku/b64.rs
@source-hash: f97ce9225f471089
@generated: 2026-02-09T18:02:16Z

## Purpose
64-bit type definitions for Haiku OS within the libc crate, providing platform-specific mappings for time and ELF binary format structures.

## Key Types and Definitions

**Basic Types (L1-5):**
- `time_t` (L1): 64-bit signed integer for timestamp representation
- `Elf_Addr`, `Elf_Half`, `Elf_Phdr` (L3-5): Type aliases mapping to corresponding 64-bit ELF types from crate root

**Structures:**
- `Elf64_Phdr` (L8-17): ELF64 program header structure containing:
  - `p_type` (L9): Program header type identifier
  - `p_flags` (L10): Access permission flags
  - `p_offset` (L11): File offset to segment data
  - `p_vaddr` (L12): Virtual address in memory
  - `p_paddr` (L13): Physical address (where relevant)
  - `p_filesz` (L14): Size of segment in file
  - `p_memsz` (L15): Size of segment in memory
  - `p_align` (L16): Alignment requirements

## Architecture Context
Part of Haiku OS 64-bit support within the libc cross-platform compatibility layer. Uses the `s!` macro (likely for structure definition with specific memory layout requirements). All ELF types reference the crate's root-level 64-bit ELF type definitions, ensuring consistency across the library.

## Dependencies
- References `crate::Elf64_*` types from libc root module
- Utilizes `s!` macro for structure definition (common pattern in libc crate for C-compatible layouts)