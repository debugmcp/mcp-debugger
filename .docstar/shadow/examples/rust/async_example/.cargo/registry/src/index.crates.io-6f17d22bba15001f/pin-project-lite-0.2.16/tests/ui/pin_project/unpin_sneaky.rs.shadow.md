# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/pin-project-lite-0.2.16/tests/ui/pin_project/unpin_sneaky.rs
@source-hash: 6e6385e067679c3b
@generated: 2026-02-09T18:02:38Z

## Purpose
Test file verifying that `pin-project-lite` prevents manual `Unpin` implementation on internal types, ensuring proper pin projection safety.

## Key Elements
- **Foo struct (L6-10)**: Simple struct with pinned `u8` field, created using `pin_project!` macro
- **Invalid Unpin impl (L12)**: Deliberately attempts to implement `Unpin` for `__Origin` (internal type generated by macro)
- **Expected compilation error**: `E0412` (cannot find type) and `E0321` (impl conflict), demonstrating the macro's protection mechanism

## Dependencies
- `pin_project_lite` crate for the `pin_project!` macro (L3)

## Test Pattern
Part of pin-project-lite's UI test suite that validates compile-time error scenarios. The test ensures users cannot circumvent the macro's safety guarantees by manually implementing `Unpin` on generated internal types.

## Architecture Notes
The `__Origin` type referenced in L12 is an internal implementation detail of the `pin_project!` macro. The test verifies this internal type is not accessible to user code, maintaining the safety invariants of pin projection.