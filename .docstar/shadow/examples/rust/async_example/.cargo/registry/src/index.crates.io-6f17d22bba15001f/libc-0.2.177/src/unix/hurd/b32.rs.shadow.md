# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/hurd/b32.rs
@source-hash: 501f426f7aeb60ac
@generated: 2026-02-09T18:02:22Z

## 32-bit GNU Hurd Type Definitions

This file provides 32-bit-specific type definitions and ELF structures for the GNU Hurd operating system within the libc crate. It serves as a platform-specific type mapping layer between C and Rust.

### Primary Responsibility
Defines platform-specific type aliases and ELF data structures for 32-bit GNU Hurd systems, ensuring proper FFI compatibility with system libraries.

### Key Type Categories

**Integer Type Mappings (L3-26)**
- 64-bit types: `__int64_t`, `__uint64_t` (L3-4) mapped to `c_longlong`/`c_ulonglong`
- Fast integer types: `int_fast16_t`, `int_fast32_t`, `int_fast64_t` (L6-11) for performance-optimized integers
- Internal system types: `__quad_t`, `__squad_type`, `__sword_type` etc. (L13-25) for low-level system operations
- IPC type: `__ipc_pid_t` (L27) as `c_ushort` for inter-process communication

**ELF 32-bit Primitive Types (L29-34)**
- Core ELF types: `Elf32_Half` (u16), `Elf32_Word` (u32), `Elf32_Off` (u32), `Elf32_Addr` (u32)
- Section indexing: `Elf32_Section` (u16)

**ELF Generic Aliases (L35-40)**
- Architecture-agnostic aliases pointing to 32-bit variants: `Elf_Addr`, `Elf_Half`, `Elf_Ehdr`, etc.

### Key Structures

**ELF32_Ehdr (L43-58)** - ELF executable header
- Contains metadata: identification, type, machine, entry point, offsets for program/section headers
- Critical fields: `e_ident[16]`, `e_entry`, `e_phoff`, `e_shoff`

**ELF32_Shdr (L60-71)** - Section header
- Describes ELF sections: name, type, flags, address, offset, size, linking info
- Key for section table parsing and linking

**ELF32_Sym (L73-80)** - Symbol table entry  
- Symbol information: name, value, size, type/binding info, section index
- Essential for dynamic linking and debugging

**ELF32_Phdr (L82-91)** - Program header
- Describes memory segments: type, file/memory offsets, virtual/physical addresses, sizes, flags
- Critical for program loading and memory layout

### Architectural Patterns
- Uses `s!` macro for struct definitions (libc crate convention)
- Consistent `crate::` prefixing for cross-references
- 32-bit specific sizing throughout (Word=u32, Half=u16, Addr=u32)
- Standard ELF specification compliance for binary format compatibility