# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/linux_like/linux/gnu/b64/s390x.rs
@source-hash: 64fba1b75736ef6e
@generated: 2026-02-09T17:57:11Z

## Architecture-Specific Linux Types and Constants for s390x

This module defines s390x (IBM z/Architecture) specific Linux types, structures, constants, and function bindings for the libc crate. It provides the low-level interface between Rust and the Linux kernel on s390x platforms.

### Primary Responsibilities
- Define s390x-specific primitive type aliases (L6-12)
- Declare system structures with correct memory layout for s390x (L14-212)
- Export platform-specific constants for system calls, signals, file operations, and error codes (L241-939)
- Provide foreign function interfaces to system calls (L941-955)

### Key Type Definitions
- **Primitive types** (L6-12): `blksize_t`, `nlink_t`, `suseconds_t`, `wchar_t`, `greg_t`, `__u64`, `__s64`
- **Signal handling**: `sigaction` struct (L17-23) with s390x-specific layout and function pointer fields
- **File system structures**: `statfs`/`statfs64` (L25-38, L183-196), `stat`/`stat64` (L70-110), `flock`/`flock64` (L40-54)
- **Process context**: `mcontext_t` (L168-173), `ucontext_t` (L175-181), `stack_t` (L64-68)
- **s390x-specific types**: `__psw_t` (L157-160) for processor status word, `fpregset_t` (L162-166) for floating point registers

### System Call Numbers
Comprehensive system call number definitions (L585-939) including:
- Core syscalls: `SYS_exit`, `SYS_fork`, `SYS_read`, `SYS_write` etc. (L585-627)
- s390x-specific: `SYS_s390_runtime_instr` (L852), `SYS_s390_pci_mmio_write/read` (L862-863)
- Modern additions: io_uring, landlock, memfd_secret syscalls (L913-939)

### Constants and Flags
- **Error codes**: Extended error code definitions (L258-429)
- **File operations**: O_* flags for file operations (L252-256, L322-344)
- **Signal handling**: SA_* flags and signal numbers (L273-453)
- **Memory mapping**: MAP_* flags (L346-358)
- **Terminal I/O**: Comprehensive termios constants (L458-583)

### External Function Bindings
Context management functions (L941-955):
- `sysctl()`: System control interface
- `getcontext()`/`setcontext()`: User context manipulation
- `makecontext()`/`swapcontext()`: Context creation and switching

### Dependencies
- Imports from parent modules: `crate::prelude::*`, specific types like `off64_t`, `pthread_mutex_t` (L3-4)
- Uses conditional compilation with `cfg_if!` macro for feature-dependent trait implementations (L222-239)

### Architecture Notes
- Uses `s!` macro for structure definitions to ensure proper alignment and layout
- Contains s390x-specific pthread mutex initializers with hardcoded byte arrays (L289-306)
- Floating point register type `fpreg_t` marked as union but implemented as struct (L214-220)