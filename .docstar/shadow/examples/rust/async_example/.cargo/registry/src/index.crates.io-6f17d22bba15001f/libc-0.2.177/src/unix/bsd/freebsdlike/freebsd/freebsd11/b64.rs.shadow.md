# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/freebsd11/b64.rs
@source-hash: 62ae6e372b644a42
@generated: 2026-02-09T17:56:52Z

## Purpose
Platform-specific definitions for FreeBSD 11 on 64-bit architectures. Part of the libc crate's BSD Unix implementation hierarchy, providing the `stat` structure layout specific to FreeBSD 11's ABI.

## Key Components

**stat struct (L4-29)**: C-compatible file metadata structure with `#[repr(C)]` layout guarantee
- Core metadata: device ID, inode, mode, ownership (uid/gid), links count
- Timestamps: access, modification, creation times with nanosecond precision
- Size information: file size, block count, block size
- FreeBSD-specific fields: flags, generation number, spare field, birth time
- Uses platform-specific type aliases (dev_t, ino_t, mode_t, etc.) from parent modules

**Clone/Copy implementations (L31-36)**: Manual implementations for `crate::stat` type
- Copy trait enables bitwise copying
- Clone implementation uses copy semantics via dereference

## Dependencies
- `crate::off_t`: File offset type from parent libc module
- `crate::prelude::*`: Standard libc type imports (c_long, primitive types)
- Various platform types: dev_t, ino_t, mode_t, nlink_t, uid_t, gid_t, time_t, blkcnt_t, blksize_t, fflags_t

## Architectural Notes
- Part of conditional compilation hierarchy: unix → bsd → freebsdlike → freebsd → freebsd11 → b64
- 64-bit specific layout matches FreeBSD 11's stat structure ABI
- Conditional trait derivation via `extra_traits` feature flag for additional trait implementations
- Manual trait implementations suggest ABI compatibility requirements over standard derive macros