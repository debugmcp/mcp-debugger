# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/haiku/b32.rs
@source-hash: c3f8678ceee65a30
@generated: 2026-02-09T18:02:15Z

**Primary Purpose:** Platform-specific type definitions for 32-bit Haiku OS within the libc crate's Unix module hierarchy. Provides ELF (Executable and Linkable Format) type aliases and structure definitions tailored for 32-bit architectures on Haiku.

**Key Type Definitions:**
- `time_t` (L1): 32-bit signed integer type for time representation, following Haiku's 32-bit time_t convention
- `Elf_Addr`, `Elf_Half`, `Elf_Phdr` (L3-5): Type aliases mapping generic ELF types to their 32-bit variants from the crate root

**Key Structures:**
- `Elf32_Phdr` (L8-17): ELF Program Header structure containing:
  - `p_type` (L9): Program header type identifier
  - `p_offset` (L10): File offset to segment data
  - `p_vaddr` (L11): Virtual memory address for segment loading
  - `p_paddr` (L12): Physical memory address (rarely used)
  - `p_filesz` (L13): Size of segment data in file
  - `p_memsz` (L14): Size of segment in memory (may exceed file size for BSS)
  - `p_flags` (L15): Segment permissions and attributes
  - `p_align` (L16): Memory/file alignment requirements

**Dependencies:**
- Relies on `crate::Elf32_*` types defined at crate root level
- Uses `s!` macro (likely from libc's internal macro system) for structure definition

**Architectural Context:**
- Part of libc's platform abstraction layer for Unix-like systems
- Specifically targets Haiku OS on 32-bit architectures
- Follows ELF specification for executable format handling
- Maintains compatibility with standard ELF toolchains and loaders