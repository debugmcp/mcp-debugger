# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/freebsd14/x86_64.rs
@source-hash: d6e66809e109dc77
@generated: 2026-02-09T17:56:50Z

## Purpose
Platform-specific constants for FreeBSD 14 x86_64 architecture, providing process control constants for Kernel Page-Table Isolation (KPTI) and Linear Address (LA) management through the procctl system call interface.

## Key Constants

### KPTI (Kernel Page-Table Isolation) Control (L3-7)
- `PROC_KPTI_CTL` (L3): Base control command for KPTI operations
- `PROC_KPTI_CTL_ENABLE_ON_EXEC` (L4): Enable KPTI on process exec (value: 1)
- `PROC_KPTI_CTL_DISABLE_ON_EXEC` (L5): Disable KPTI on process exec (value: 2)
- `PROC_KPTI_STATUS` (L6): Query KPTI status command
- `PROC_KPTI_STATUS_ACTIVE` (L7): Bitmask indicating active KPTI state (0x80000000)

### Linear Address Control (L8-14)
- `PROC_LA_CTL` (L8): Base control command for linear address management
- `PROC_LA_STATUS` (L9): Query linear address status command
- `PROC_LA_CTL_LA48_ON_EXEC` (L10): Enable 48-bit linear addressing on exec (value: 1)
- `PROC_LA_CTL_LA57_ON_EXEC` (L11): Enable 57-bit linear addressing on exec (value: 2)
- `PROC_LA_CTL_DEFAULT_ON_EXEC` (L12): Use default linear addressing on exec (value: 3)
- `PROC_LA_STATUS_LA48` (L13): Bitmask for 48-bit LA status (0x01000000)
- `PROC_LA_STATUS_LA57` (L14): Bitmask for 57-bit LA status (0x02000000)

## Dependencies
- `crate::prelude::*` (L1): Standard type definitions including `c_int`
- `crate::PROC_PROCCTL_MD_MIN`: Machine-dependent base offset for procctl commands

## Architecture Notes
- Commands use sequential offsets from `PROC_PROCCTL_MD_MIN` base
- Status values use high-order bits as flags for efficient bitwise operations
- Specific to x86_64 architecture supporting both 48-bit and 57-bit linear addressing modes
- Part of FreeBSD's process control interface for security and memory management features