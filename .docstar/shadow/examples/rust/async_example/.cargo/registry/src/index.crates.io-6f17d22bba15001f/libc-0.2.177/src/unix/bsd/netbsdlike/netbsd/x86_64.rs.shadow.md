# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/netbsdlike/netbsd/x86_64.rs
@source-hash: cb864e23a32eff1b
@generated: 2026-02-09T17:57:13Z

## NetBSD x86_64 Platform-Specific C Interface Definitions

**Primary Purpose:** Provides x86_64 architecture-specific type definitions, structures, and constants for NetBSD system interface compatibility within the libc FFI bindings.

### Key Type Definitions
- `c___greg_t` (L4): 64-bit unsigned integer type for general-purpose registers
- `__cpu_simple_lock_nv_t` (L5): Single-byte lock type using `c_uchar`

### Core Data Structures
- `mcontext_t` (L8-12): Machine context structure containing:
  - `__gregs`: Array of 26 general-purpose registers (64-bit each)
  - `_mc_tlsbase`: Thread-local storage base register
  - `__fpregs`: 16 floating-point registers (32 bytes each)

- `ucontext_t` (L14-20): User context structure for signal handling and context switching:
  - Links to signal mask, stack info, and machine context
  - Uses cross-references to parent crate types (`crate::ucontext_t`, `crate::sigset_t`, etc.)

### System Constants
- `_ALIGNBYTES` (L23): Memory alignment constant based on `c_long` size
- **Process Tracing Constants** (L25-29): ptrace operation codes built on `PT_FIRSTMACH` base
- **x86_64 Register Index Constants** (L31-56): Complete mapping of x86_64 register indices (_REG_RDI through _REG_SS)

### Architecture Details
- Uses `s!` macro for structure definitions (libc crate convention)
- Register layout follows x86_64 System V ABI conventions
- 26-register general-purpose register array matches NetBSD's x86_64 register state representation

### Dependencies
- Imports from `crate::prelude::*` and `crate::PT_FIRSTMACH`
- Cross-references several parent crate types for ABI consistency