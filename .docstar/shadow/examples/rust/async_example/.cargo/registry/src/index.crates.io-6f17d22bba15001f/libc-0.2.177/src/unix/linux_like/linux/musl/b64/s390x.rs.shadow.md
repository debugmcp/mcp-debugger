# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/linux_like/linux/musl/b64/s390x.rs
@source-hash: 30f9ac1527e49a57
@generated: 2026-02-09T17:57:07Z

## s390x-specific Linux/musl type definitions and constants

**Purpose**: Platform-specific type definitions, structures, and constants for s390x (IBM System z 64-bit) architecture running Linux with musl libc. This file provides the low-level system interface bindings required for system calls and C library compatibility.

### Core Type Definitions (L4-10)
- Platform-specific primitive types: `blksize_t` (i64), `nlink_t` (u64), `wchar_t` (i32), `greg_t` (u64)
- Kernel interface types: `__u64`, `__s64` for direct kernel communication
- `statfs64` aliased to `statfs` indicating this platform uses 64-bit filesystem structures

### Key System Structures

**IPC Permissions Structure** (`ipc_perm`, L13-31)
- Inter-process communication permission structure with version-dependent field naming
- Contains conditional compilation for `__key` vs `__ipc_perm_key` field based on musl version
- Standard IPC fields: uid/gid pairs, mode, sequence number

**File System Structures** (L33-86)
- `stat` (L33-51): Standard file metadata structure with nanosecond-precision timestamps
- `stat64` (L53-71): 64-bit version using `ino64_t` and `blkcnt64_t` for large file support  
- `statfs` (L73-86): Filesystem statistics structure with block/inode counts and filesystem info

**Floating Point Register Union** (`fpreg_t`, L90-93)
- Union allowing access to floating-point registers as either double or float
- Wrapped in `s_no_extra_traits!` macro to exclude from automatic trait derivation

### Trait Implementations (L96-112)
Conditional trait implementations for `fpreg_t` when "extra_traits" feature is enabled:
- `PartialEq`, `Eq`, `Hash` traits with unimplemented bodies (placeholder for union types)

### System Constants

**Error Codes** (L116-240)
- Comprehensive errno definitions for s390x including Linux-specific and network error codes
- Notable s390x-specific codes and standard POSIX error numbers

**File Operation Flags** (L130-153) 
- File descriptor flags: O_APPEND, O_CREAT, O_LARGEFILE, O_NONBLOCK, etc.
- Architecture-specific values for open/fcntl operations

**Signal Constants** (L135-263)
- Signal action flags, signal numbers, and signal mask operations
- s390x-specific signal stack sizes: SIGSTKSZ (0x2000), MINSIGSTKSZ (2048)

**Terminal I/O Constants** (L266-357)
- Comprehensive termios flags for serial/terminal control
- Baud rate definitions including high-speed rates up to 4Mbps
- Control character indices and terminal mode flags

**Memory Management** (L154-166)
- Memory mapping flags (MAP_*) and memory advice constants (MADV_*)
- Platform-specific memory management behavior controls

### System Call Numbers (L359-732)
Complete system call number definitions for s390x architecture:
- Classic UNIX calls (exit, fork, read, write, etc.)
- Linux-specific extensions (epoll, io_uring, landlock, etc.)
- s390x-specific calls: `SYS_s390_runtime_instr`, `SYS_s390_pci_mmio_*`, `SYS_s390_guarded_storage`

**Dependencies**: 
- `crate::off_t`, `crate::prelude::*` for base type definitions
- Conditional compilation based on musl version and feature flags