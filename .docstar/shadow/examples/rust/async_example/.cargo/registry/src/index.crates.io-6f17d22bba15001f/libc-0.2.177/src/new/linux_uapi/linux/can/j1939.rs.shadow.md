# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/new/linux_uapi/linux/can/j1939.rs
@source-hash: ab2a330c3d3cca3a
@generated: 2026-02-09T17:57:12Z

This file provides Rust FFI bindings for the Linux J1939 CAN protocol interface, exposing constants and data structures from `linux/can/j1939.h`.

## Purpose
Defines J1939 (vehicle networking protocol) constants, types, and structures for Linux CAN socket programming in Rust. J1939 is a higher-layer protocol built on top of CAN bus used in heavy-duty vehicles and industrial applications.

## Key Elements

**Core Address Constants (L5-8):**
- `J1939_MAX_UNICAST_ADDR` (0xfd) - Maximum valid unicast address
- `J1939_IDLE_ADDR` (0xfe) - Reserved idle address  
- `J1939_NO_ADDR` (0xff) - Indicates no address assigned
- `J1939_NO_NAME` (0) - Indicates no name assigned

**Parameter Group Number (PGN) Constants (L9-14):**
- `J1939_PGN_REQUEST` (0x0ea00) - Standard request PGN
- `J1939_PGN_ADDRESS_CLAIMED` (0x0ee00) - Address claim PGN
- `J1939_PGN_ADDRESS_COMMANDED` (0x0fed8) - Address command PGN
- `J1939_PGN_PDU1_MAX`/`J1939_PGN_MAX` - PGN range limits
- `J1939_NO_PGN` (0x40000) - Invalid PGN marker

**Type Aliases (L16-18):**
- `pgn_t` = u32 - Parameter Group Number type
- `priority_t` = u8 - Message priority type  
- `name_t` = u64 - J1939 name type

**Socket Options (L24-27):**
- `SO_J1939_FILTER` - Message filtering
- `SO_J1939_PROMISC` - Promiscuous mode
- `SO_J1939_SEND_PRIO` - Send priority
- `SO_J1939_ERRQUEUE` - Error queue control

**Control Message Types (L29-32):**
- `SCM_J1939_DEST_ADDR/NAME` - Destination addressing
- `SCM_J1939_PRIO` - Priority control
- `SCM_J1939_ERRQUEUE` - Error queue messages

**Netlink Attributes (L34-41):**
Constants for J1939 netlink attribute types (padding, acknowledgment, addressing, etc.)

**Error Queue Info Types (L43-47):**
Constants for different J1939 error/event types (TX abort, RX events, etc.)

**Core Data Structure:**
- `j1939_filter` struct (L50-57) - Defines message filtering criteria with name, PGN, and address fields plus corresponding masks

**Dependencies:**
- Re-exports all items from `crate::linux::can` (L3)
- Uses `SOL_CAN_BASE` and `CAN_J1939` for socket level definition (L20)

**Constraints:**
- Maximum filter count limited to 512 (`J1939_FILTER_MAX`, L60)
- Uses standard C integer types for Linux kernel compatibility