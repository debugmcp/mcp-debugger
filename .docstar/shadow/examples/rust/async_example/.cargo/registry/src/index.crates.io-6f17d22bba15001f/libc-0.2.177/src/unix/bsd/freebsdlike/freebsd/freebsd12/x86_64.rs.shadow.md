# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/freebsd12/x86_64.rs
@source-hash: 64c4bd82eaf30a26
@generated: 2026-02-09T17:56:50Z

**Primary Purpose**: FreeBSD 12 x86_64-specific system constants for Kernel Page Table Isolation (KPTI) process control operations.

**Architecture Context**: Part of libc's Unix BSD FreeBSD platform-specific definitions, providing low-level system interface constants for x86_64 architecture on FreeBSD 12.

**Key Constants**:
- `PROC_KPTI_CTL` (L3): Base control constant for KPTI operations, derived from `PROC_PROCCTL_MD_MIN`
- `PROC_KPTI_CTL_ENABLE_ON_EXEC` (L4): Flag value (1) to enable KPTI on process execution
- `PROC_KPTI_CTL_DISABLE_ON_EXEC` (L5): Flag value (2) to disable KPTI on process execution  
- `PROC_KPTI_STATUS` (L6): Status query constant, offset by 1 from base control constant
- `PROC_KPTI_STATUS_ACTIVE` (L7): Bitmask (0x80000000) indicating active KPTI state

**Dependencies**: 
- `crate::prelude::*` (L1): Standard libc type definitions
- `crate::PROC_PROCCTL_MD_MIN`: Machine-dependent process control minimum value

**Usage Pattern**: These constants are typically used with FreeBSD's `procctl()` system call to control and query KPTI (security mitigation against Meltdown/Spectre) behavior for processes on x86_64 systems.

**Security Context**: KPTI constants enable runtime control of kernel page table isolation, a critical security feature for mitigating side-channel attacks.