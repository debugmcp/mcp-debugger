# examples/rust/async_example/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/x86.rs
@source-hash: a3a17037050ba9f2
@generated: 2026-02-09T17:57:14Z

## Purpose and Responsibility
FreeBSD x86 (32-bit) architecture-specific type definitions and constants for the libc crate. Provides low-level C FFI bindings for system types, machine context structures, and platform-specific constants used in FreeBSD system programming on x86 architecture.

## Key Type Definitions (L3-7)
- `clock_t`, `wchar_t`, `time_t`, `suseconds_t`: Standard C types mapped to appropriate Rust primitives
- `register_t`: i32 type representing CPU register values in x86 context

## Core Structure
- **mcontext_t (L10-42)**: Machine context structure representing complete CPU state
  - 16-byte aligned (`#[repr(align(16))]`)
  - Contains all x86 general-purpose registers (eax, ebx, ecx, etc.)
  - Includes segment registers (gs, fs, es, ds, cs, ss)
  - FPU state management fields (mc_fpformat, mc_ownedfp, mc_fpstate[128])
  - Extended FPU state (mc_xfpustate, mc_xfpustate_len)
  - Signal handling state (mc_onstack)
  - Uses `s_no_extra_traits!` macro to conditionally exclude trait derivations

## Conditional Trait Implementations (L45-125)
- **PartialEq implementation (L47-88)**: Field-by-field comparison including array element comparison for mc_fpstate and mc_spare2
- **Hash implementation (L90-123)**: Comprehensive hashing of all struct fields
- Gated behind "extra_traits" feature flag using `cfg_if!` macro

## Platform Constants (L127-134)
- `_ALIGNBYTES`: Memory alignment constant based on c_long size
- `MINSIGSTKSZ`: Minimum signal stack size (2048 bytes)
- `BIOCSRTIMEOUT`/`BIOCGRTIMEOUT`: Berkeley Packet Filter timeout ioctl constants
- `KINFO_FILE_SIZE`: Kernel file info structure size
- `TIOCTIMESTAMP`: Terminal timestamp ioctl constant

## Dependencies
- Uses `crate::prelude::*` for common libc types and macros
- Relies on `s_no_extra_traits!`, `cfg_if!` macros from parent crate
- Standard library `hash` module for Hash trait

## Architectural Notes
- x86-specific 32-bit register layout with 16-byte alignment requirement
- FPU state handling includes both legacy (mc_fpstate) and extended (mc_xfpustate) formats
- Feature-gated trait implementations allow for minimal builds when traits aren't needed