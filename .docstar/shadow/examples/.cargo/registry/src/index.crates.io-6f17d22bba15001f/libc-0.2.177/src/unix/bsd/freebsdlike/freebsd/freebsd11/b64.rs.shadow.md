# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/freebsd11/b64.rs
@source-hash: 62ae6e372b644a42
@generated: 2026-02-09T17:58:02Z

## Purpose
Platform-specific file metadata structure for FreeBSD 11 on 64-bit architectures. Part of libc crate's Unix BSD variant hierarchy providing system-level file stat functionality.

## Key Structures
- **stat (L4-29)**: C-compatible file metadata structure with comprehensive file information
  - Device and inode identification (`st_dev`, `st_ino`)
  - File permissions and type (`st_mode`)
  - Ownership information (`st_uid`, `st_gid`, `st_nlink`)
  - Timestamps with nanosecond precision (access, modification, change, birth times)
  - File size and block allocation details (`st_size`, `st_blocks`, `st_blksize`)
  - FreeBSD-specific fields (`st_flags`, `st_gen`, `st_lspare`)

## Trait Implementations
- **Copy trait (L31)**: Enables bitwise copying for `crate::stat`
- **Clone trait (L32-36)**: Manual implementation using copy semantics
- Conditional traits via `extra_traits` feature: `Eq`, `Hash`, `PartialEq` (L6)

## Dependencies
- Uses crate-level type aliases (`off_t`, `dev_t`, `ino_t`, etc.)
- Imports prelude for common C types (`c_long`)

## Architecture Notes
- `#[repr(C)]` ensures C ABI compatibility for system call interop
- Structure layout matches FreeBSD 11 kernel's stat structure exactly
- 64-bit specific variant (indicated by `b64` in filename)
- Part of hierarchical platform abstraction: unix → bsd → freebsdlike → freebsd → freebsd11 → b64