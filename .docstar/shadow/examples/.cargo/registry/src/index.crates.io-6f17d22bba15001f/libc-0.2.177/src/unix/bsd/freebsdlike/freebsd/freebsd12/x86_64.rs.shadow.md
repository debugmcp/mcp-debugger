# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/freebsd12/x86_64.rs
@source-hash: 64c4bd82eaf30a26
@generated: 2026-02-09T17:58:01Z

## Purpose and Responsibility

FreeBSD 12 x86_64-specific system constants for Kernel Page Table Isolation (KPTI) process control. This file provides low-level constants used by the `procctl()` system call to manage KPTI settings for processes on FreeBSD 12 x86_64 architecture.

## Key Constants

- `PROC_KPTI_CTL` (L3): Base control constant for KPTI operations, derived from `PROC_PROCCTL_MD_MIN`
- `PROC_KPTI_CTL_ENABLE_ON_EXEC` (L4): Flag value (1) to enable KPTI on process execution
- `PROC_KPTI_CTL_DISABLE_ON_EXEC` (L5): Flag value (2) to disable KPTI on process execution  
- `PROC_KPTI_STATUS` (L6): Status query constant, offset by 1 from base control constant
- `PROC_KPTI_STATUS_ACTIVE` (L7): Bitmask (0x80000000) indicating KPTI is currently active

## Dependencies

- `crate::prelude::*` (L1): Standard libc prelude imports
- `crate::PROC_PROCCTL_MD_MIN`: Base value for machine-dependent procctl operations

## Architectural Context

Part of FreeBSD's libc binding hierarchy: `unix/bsd/freebsdlike/freebsd/freebsd12/x86_64.rs`. These constants enable userspace programs to control KPTI (security mitigation against Meltdown vulnerability) via the `procctl()` system call on FreeBSD 12 x86_64 systems.

## Usage Pattern

Constants follow FreeBSD's procctl API convention where control operations use sequential values from a base constant, and status operations use separate offset constants with bitmask values for flags.