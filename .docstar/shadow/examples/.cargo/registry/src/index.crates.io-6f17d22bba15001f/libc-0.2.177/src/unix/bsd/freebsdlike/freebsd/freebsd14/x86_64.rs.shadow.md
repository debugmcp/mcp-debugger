# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/freebsd14/x86_64.rs
@source-hash: d6e66809e109dc77
@generated: 2026-02-09T17:58:04Z

**Primary Purpose**: FreeBSD 14 x86_64 architecture-specific process control constants for kernel page table isolation (KPTI) and linear address (LA) extensions.

**Architecture Context**: Part of libc's BSD-like FreeBSD platform-specific bindings, targeting FreeBSD 14 on x86_64 architecture. Extends the base FreeBSD definitions with architecture-specific process control capabilities.

**Key Constants**:

**KPTI (Kernel Page Table Isolation) Control (L3-7)**:
- `PROC_KPTI_CTL` (L3): Base control command using `PROC_PROCCTL_MD_MIN`
- `PROC_KPTI_CTL_ENABLE_ON_EXEC` (L4): Enable KPTI on exec (value: 1)
- `PROC_KPTI_CTL_DISABLE_ON_EXEC` (L5): Disable KPTI on exec (value: 2)
- `PROC_KPTI_STATUS` (L6): Status query command (`PROC_PROCCTL_MD_MIN + 1`)
- `PROC_KPTI_STATUS_ACTIVE` (L7): Active status flag (0x80000000)

**Linear Address Extension Control (L8-14)**:
- `PROC_LA_CTL` (L8): LA control command (`PROC_PROCCTL_MD_MIN + 2`)
- `PROC_LA_STATUS` (L9): LA status query (`PROC_PROCCTL_MD_MIN + 3`)
- `PROC_LA_CTL_LA48_ON_EXEC` (L10): Enable 48-bit LA on exec
- `PROC_LA_CTL_LA57_ON_EXEC` (L11): Enable 57-bit LA on exec
- `PROC_LA_CTL_DEFAULT_ON_EXEC` (L12): Use default LA mode on exec
- `PROC_LA_STATUS_LA48` (L13): 48-bit LA active status (0x01000000)
- `PROC_LA_STATUS_LA57` (L14): 57-bit LA active status (0x02000000)

**Dependencies**: 
- Uses `crate::prelude::*` (L1) for common type definitions
- References `crate::PROC_PROCCTL_MD_MIN` as base offset for machine-dependent procctl commands

**Architectural Pattern**: Sequential constant enumeration pattern with bitflag status values for process control system calls, specifically for security and memory management features introduced in FreeBSD 14.

**Usage Context**: These constants are used with FreeBSD's `procctl()` system call to control KPTI (Meltdown/Spectre mitigation) and linear address space extensions on x86_64 processors.