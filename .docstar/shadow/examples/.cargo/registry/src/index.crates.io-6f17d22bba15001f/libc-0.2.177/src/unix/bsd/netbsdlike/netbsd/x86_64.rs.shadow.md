# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/netbsdlike/netbsd/x86_64.rs
@source-hash: cb864e23a32eff1b
@generated: 2026-02-09T18:02:12Z

## Purpose
NetBSD x86_64 architecture-specific system bindings providing CPU context structures, processor debugging constants, and register definitions for the libc crate.

## Key Components

### Type Definitions (L4-5)
- `c___greg_t` (L4): 64-bit unsigned integer type for general-purpose registers on x86_64
- `__cpu_simple_lock_nv_t` (L5): Single byte type for CPU simple locks

### Core Structures (L7-21)
- `mcontext_t` (L8-12): Machine context structure containing:
  - `__gregs` (L9): Array of 26 general-purpose registers
  - `_mc_tlsbase` (L10): Thread-local storage base register
  - `__fpregs` (L11): 16 floating-point registers, 32 bytes each
- `ucontext_t` (L14-20): User context structure for signal handling and context switching:
  - `uc_flags` (L15): Context flags
  - `uc_link` (L16): Pointer to next context in chain
  - `uc_sigmask` (L17): Signal mask
  - `uc_stack` (L18): Stack information
  - `uc_mcontext` (L19): Machine-specific context

### Constants
- `_ALIGNBYTES` (L23): Memory alignment constant based on `c_long` size
- **Ptrace Operations** (L25-29): Process tracing constants for debugging:
  - `PT_STEP`, `PT_GETREGS`, `PT_SETREGS`, `PT_GETFPREGS`, `PT_SETFPREGS`
- **Register Indices** (L31-56): x86_64 register enumeration constants mapping to array indices in `__gregs`, covering general-purpose registers, segment registers, and processor state

## Dependencies
- `crate::prelude::*` (L1): Core type definitions
- `crate::PT_FIRSTMACH` (L2): Base constant for machine-specific ptrace operations

## Architecture Notes
- Designed specifically for NetBSD on x86_64 architecture
- Register layout matches standard x86_64 ABI conventions
- Context structures support signal handling and process debugging workflows