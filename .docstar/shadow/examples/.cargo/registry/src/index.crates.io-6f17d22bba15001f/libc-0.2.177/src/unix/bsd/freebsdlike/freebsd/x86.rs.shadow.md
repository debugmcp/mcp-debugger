# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/x86.rs
@source-hash: a3a17037050ba9f2
@generated: 2026-02-09T18:02:14Z

This file provides x86-specific type definitions and constants for FreeBSD system programming within the libc crate. It serves as the x86 (32-bit) architecture layer in the FreeBSD-like BSD Unix system bindings hierarchy.

## Type Definitions (L3-7)
- `clock_t`, `wchar_t`, `time_t`, `suseconds_t`: Standard C types mapped to appropriate Rust primitives for x86
- `register_t`: Base type for CPU register values, defined as `i32` for x86 architecture

## Core Structure
**`mcontext_t` (L10-42)**: Machine context structure representing complete x86 CPU state for signal handling and context switching. 16-byte aligned structure containing:
- x86 general-purpose registers (`mc_eax`, `mc_ebx`, `mc_ecx`, etc.)
- Segment registers (`mc_cs`, `mc_ds`, `mc_es`, `mc_fs`, `mc_gs`, `mc_ss`)
- Stack and instruction pointers (`mc_esp`, `mc_eip`, `mc_ebp`)
- Control flags (`mc_eflags`, `mc_flags`)
- Floating-point state (`mc_fpstate`, `mc_fpformat`, `mc_ownedfp`)
- Extended FPU state (`mc_xfpustate`, `mc_xfpustate_len`)
- Base addresses for segment registers (`mc_fsbase`, `mc_gsbase`)

## Conditional Traits (L45-125)
When `extra_traits` feature is enabled, implements comparison and hashing traits for `mcontext_t`:
- `PartialEq` (L47-88): Field-by-field comparison including array comparisons for `mc_fpstate` and `mc_spare2`
- `Eq` (L89): Marker trait
- `Hash` (L90-123): Comprehensive hashing of all fields

## Constants
- `_ALIGNBYTES` (L127): Memory alignment constant for the platform
- `MINSIGSTKSZ` (L129): Minimum signal stack size (2048 bytes)
- Berkeley Packet Filter constants (L131-132): `BIOCSRTIMEOUT`, `BIOCGRTIMEOUT` for network packet capture timeouts
- `KINFO_FILE_SIZE` (L133): Size of kernel file information structure
- `TIOCTIMESTAMP` (L134): Terminal I/O control constant for timestamping

## Architecture Notes
This file specifically targets x86 (32-bit) FreeBSD systems. The register names and structure layout match x86 processor architecture, with 32-bit register types and x86-specific segment registers. The `mcontext_t` structure is critical for low-level system operations like signal handling and user-space threading.