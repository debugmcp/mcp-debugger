# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/freebsd11/b32.rs
@source-hash: 951c9297ed31a135
@generated: 2026-02-09T17:58:01Z

**Primary Purpose**: Defines FreeBSD 11 32-bit specific filesystem stat structure and associated trait implementations.

**Architecture Context**: Part of libc crate's platform-specific hierarchy: unix → bsd → freebsdlike → freebsd → freebsd11 → b32, indicating this is the 32-bit variant for FreeBSD 11.

**Key Components**:

- **`stat` struct (L4-30)**: C-compatible filesystem metadata structure with `#[repr(C)]` for direct FFI usage
  - Standard POSIX fields: device ID, inode, permissions, ownership, timestamps  
  - FreeBSD-specific extensions: `st_flags` (L24), `st_gen` (L25), `st_birthtime` (L27-28)
  - Nanosecond precision timestamps via `_nsec` fields (L16, 18, 20, 28)
  - Private padding field `__unused` (L29) for ABI alignment

- **Trait Implementations**:
  - **Copy/Clone (L32-37)**: Manual implementations using bitwise copy semantics
  - **Conditional traits (L6)**: Debug always enabled, Eq/Hash/PartialEq behind `extra_traits` feature flag

**Dependencies**: 
- `crate::off_t` (L1) - file offset type
- `crate::prelude::*` (L2) - common type aliases (c_long, primitive types)
- Various libc type aliases (dev_t, ino_t, mode_t, etc.)

**Critical Constraints**:
- ABI compatibility with FreeBSD 11 32-bit kernel structures
- Field order and sizes must exactly match system headers
- `#[repr(C)]` ensures no Rust field reordering