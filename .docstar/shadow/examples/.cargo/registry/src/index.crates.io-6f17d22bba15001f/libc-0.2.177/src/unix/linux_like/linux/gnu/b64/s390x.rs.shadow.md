# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/linux_like/linux/gnu/b64/s390x.rs
@source-hash: 64fba1b75736ef6e
@generated: 2026-02-09T17:58:29Z

## s390x Architecture Linux System Bindings

This file provides s390x (IBM System/390 64-bit) architecture-specific type definitions, constants, and system call bindings for Linux with GNU libc. Part of the libc crate's Unix platform support hierarchy.

### Core Type Definitions (L6-L12)
- `blksize_t`, `nlink_t`, `suseconds_t`: Basic filesystem and time types
- `wchar_t`: Wide character type (32-bit)
- `greg_t`: General register type (64-bit)
- `__u64`, `__s64`: Kernel interface types

### Key Structures

**Signal Handling (L17-L23)**
- `sigaction`: Signal handler configuration with s390x-specific layout
- Contains deprecation warning about `PartialEq` implementation (L15)

**File System Operations (L25-L55)**
- `statfs`/`statfs64` (L25-L38, L183-L196): File system statistics
- `statvfs`/`statvfs64` (L142-L155, L198-L211): POSIX filesystem info
- `flock`/`flock64` (L40-L54): File locking structures with 32/64-bit offset variants

**File Metadata (L70-L110)**
- `stat`/`stat64`: File status structures with nanosecond timestamp precision
- Uses s390x-specific field ordering and padding

**Process Context (L157-L181)**
- `__psw_t` (L157-L160): Program Status Word (s390x processor state)
- `fpregset_t` (L162-L166): Floating point register set
- `mcontext_t` (L168-L173): Machine context with general/address/floating-point registers
- `ucontext_t` (L175-L181): Complete user context for signal handling

**IPC/Threading (L112-L140)**
- `pthread_attr_t`: Thread attributes (7 64-bit words)
- `ipc_perm`/`shmid_ds`: System V IPC structures

**Special Float Handling (L214-L239)**
- `fpreg_t`: Floating-point register union (marked as union but implemented as struct)
- Conditional trait implementations based on `extra_traits` feature

### Constants

**POSIX Advisory (L241-L242)**
- File advisory constants specific to s390x

**File Operations (L252-L256, L322-L344)**
- File creation/access flags with s390x-specific values
- Includes modern additions like `O_TMPFILE`

**Error Codes (L258-L429)**
- Complete errno definitions for s390x Linux
- Network, filesystem, and system-specific error values

**Signal Constants (L273-L277, L431-L453)**
- Signal handling flags and signal numbers
- Includes s390x-specific signal stack size (`SIGSTKSZ`: 8KB, L330)

**Memory Management (L346-L358)**
- `mmap()` flags and memory advisory constants
- s390x-specific memory mapping options

**Terminal I/O (L458-L583)**
- Comprehensive termios constants for serial/terminal control
- Baud rates, control characters, and flags

**System Calls (L585-L939)**
- Complete s390x syscall number definitions
- Covers traditional syscalls through modern additions (io_uring, landlock, etc.)
- Some deprecated syscalls marked with version notes

**Pthread Synchronization (L282-L306)**
- Mutex size constants and specialized mutex initializers
- s390x-specific pthread object sizes

### External Functions (L941-L955)
- Context manipulation: `getcontext`, `setcontext`, `makecontext`, `swapcontext`
- System control: `sysctl`

### Architecture Notes
- All structures use s390x-specific field alignment and padding
- 64-bit architecture with big-endian considerations
- IBM mainframe-specific processor state handling
- Comprehensive syscall coverage including z/Architecture extensions