# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/unix/bsd/freebsdlike/freebsd/freebsd13/x86_64.rs
@source-hash: 64c4bd82eaf30a26
@generated: 2026-02-09T17:58:01Z

**Purpose**: Platform-specific constants for FreeBSD 13 x86_64 architecture, providing KPTI (Kernel Page Table Isolation) process control definitions.

**Key Elements**:
- **PROC_KPTI_CTL (L3)**: Base constant for KPTI control operations, derived from `PROC_PROCCTL_MD_MIN`
- **PROC_KPTI_CTL_ENABLE_ON_EXEC (L4)**: Flag value (1) to enable KPTI on process execution
- **PROC_KPTI_CTL_DISABLE_ON_EXEC (L5)**: Flag value (2) to disable KPTI on process execution
- **PROC_KPTI_STATUS (L6)**: Status query constant, offset by +1 from base control constant
- **PROC_KPTI_STATUS_ACTIVE (L7)**: Bitmask (0x80000000) indicating active KPTI status

**Dependencies**: 
- Imports crate prelude (L1)
- References `PROC_PROCCTL_MD_MIN` from parent crate scope

**Architecture Context**: 
This is part of the libc crate's BSD/FreeBSD hierarchy, specifically targeting FreeBSD 13 on x86_64. The constants enable userspace programs to control and query KPTI (Kernel Page Table Isolation) settings via the procctl() system call, which is a security feature that mitigates certain CPU vulnerabilities by isolating kernel and user page tables.

**Usage Pattern**: These constants would typically be used with FreeBSD's procctl() system call to manage KPTI behavior for processes.