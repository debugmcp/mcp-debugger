# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/libc-0.2.177/src/fuchsia/x86_64.rs
@source-hash: a6de0a3c9a45e7af
@generated: 2026-02-09T18:06:23Z

## Fuchsia x86_64 Platform Bindings

Platform-specific type definitions and system structures for Fuchsia OS on x86_64 architecture within the libc crate. This module provides C ABI compatibility for system calls and data structures.

### Type Aliases (L4-7)
- `wchar_t = i32` - Wide character type
- `nlink_t = u64` - Hard link count type  
- `blksize_t = c_long` - Block size type
- `__u64 = c_ulonglong` - Internal 64-bit unsigned type

### Core Structures

**stat (L10-29)**: Standard file status structure with all standard Unix stat fields including device ID, inode, permissions, ownership, size, and timestamps. Contains architecture-specific padding (`__pad0`, `__unused`).

**stat64 (L31-50)**: 64-bit version of stat structure. Nearly identical to stat but uses `ino64_t` for inode and `blkcnt64_t` for block count. Uses `__reserved` instead of `__unused` for padding.

**mcontext_t (L52-54)**: Machine context structure for signal handling, containing 32 64-bit values as opaque private data.

**ipc_perm (L56-66)**: System V IPC permissions structure with user/group IDs, permissions mode, and sequence number.

**ucontext_t (L70-77)**: User context structure for signal handling containing flags, linked context pointer, signal stack, machine context, and signal mask. Defined with `s_no_extra_traits!` macro to exclude automatic trait derivations.

### Conditional Trait Implementations (L80-108)
When `extra_traits` feature is enabled, provides manual implementations of:
- `PartialEq` for `ucontext_t` - compares all fields including private data array
- `Eq` for `ucontext_t` 
- `Hash` for `ucontext_t` - hashes all fields in sequence

### x86_64 Register Constants (L111-137)
Defines register offset constants for `user_regs_struct` from `sys/reg.h`:
- General purpose registers (RAX, RBX, RCX, etc.)
- Segment registers (CS, DS, ES, FS, GS, SS)
- Special registers (RIP, RSP, EFLAGS, ORIG_RAX)
- Base registers (FS_BASE, GS_BASE)

### Platform Constants (L139-142)
- `MAP_32BIT = 0x0040` - Memory mapping flag for 32-bit address space
- `SIGSTKSZ = 8192` - Default signal stack size
- `MINSIGSTKSZ = 2048` - Minimum signal stack size

### Dependencies
- Imports `off_t` and common prelude types from parent crate
- Uses `cfg_if!` macro for conditional compilation
- Relies on `s!` and `s_no_extra_traits!` macros for structure definitions