# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/syn-2.0.110/src/gen/clone.rs
@source-hash: 7af00b0a240453d7
@generated: 2026-02-09T18:06:28Z

**Primary Purpose:** Generated Clone trait implementations for syn crate's AST types.

**Key Architecture:**
- Code generation: Automatically generated by `syn-internal-codegen` (L1-2), not for manual editing
- Feature-gated implementations: All Clone implementations are conditionally compiled based on crate features (`derive`, `full`)
- Documentation gating: All impls have `#[cfg_attr(docsrs, doc(cfg(feature = "clone-impls")))]` for docs.rs

**Implementation Patterns:**
- **Manual Clone for structs**: Field-by-field cloning (e.g., `Abi` L7-14, `AngleBracketedGenericArguments` L17-26)
- **Copy + Clone for enums**: Simple copy semantics for small enums (e.g., `AttrStyle` L67-74, `BinOp` L112-119)
- **Enum variant matching**: Recursive cloning for complex enums (e.g., `Expr` L239-310, `Type` L1919-1939)
- **Empty enum handling**: Match with empty arms for uninhabited types (e.g., `ImplRestriction` L1070-1074)

**Feature Dependencies:**
- `feature = "derive"`: Types used in derive macros (L181-236)
- `feature = "full"`: Full language parsing support (L27, L120, etc.)
- `any(feature = "derive", feature = "full")`: Common types (L5-14, L41-52)

**Major Type Categories:**
- **AST Nodes**: Expression types (`Expr*` L239-780), Item types (`Item*` L1087-1338), Pattern types (`Pat*` L1491-1641)
- **Syntax Elements**: Tokens, punctuation, and delimiters
- **Generics**: Type parameters, bounds, constraints (L946-994, L2017-2049)
- **Metadata**: Attributes, visibility, documentation

**Critical Implementation Details:**
- Unreachable branches for non-full features (L307, L2046) - ensures compile-time safety
- Recursive cloning maintains AST structure integrity
- All token and span information is preserved through cloning
- Field-level cloning respects Option types and collections