# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/syn-2.0.110/src/gen/hash.rs
@source-hash: 6808bb0e47e7346a
@generated: 2026-02-09T18:06:35Z

**Generated Hash Implementations for Syn AST Types**

This code-generated file implements the `std::hash::Hash` trait for all AST node types in the syn crate, enabling these types to be used as keys in hash-based collections.

## Primary Purpose
Provides complete Hash trait implementations for syn's Abstract Syntax Tree (AST) types, conditionally compiled based on feature flags ("derive", "full", "extra-traits").

## Architecture Pattern
- **Code Generation**: File is @generated by syn-internal-codegen (L1-2)
- **Conditional Compilation**: Heavy use of `#[cfg()]` attributes for feature-gated implementations
- **Consistent Hashing Strategy**: 
  - Enum variants use `state.write_u8()` with unique discriminants for type safety
  - Struct fields are hashed in declaration order
  - Special handling for `TokenStream` via `TokenStreamHelper` wrapper (L5, L526, L1166, etc.)

## Key Implementation Categories

### Core Expression Types (L346-542)
- `Expr` enum with 40+ variants (L348-542), each tagged with unique u8 discriminant
- Individual expression structs like `ExprBinary` (L591-601), `ExprCall` (L628-637)
- Feature-gated variants use `#[cfg(feature = "full")]`

### Type System (L2428-2689)
- `Type` enum with 15+ variants (L2433-2495)
- Concrete type implementations: `TypeArray` (L2499-2507), `TypePath` (L2626-2634)
- Generic parameter handling: `TypeParam` (L2571-2583)

### Pattern Matching (L1874-2080)
- `Pat` enum with comprehensive pattern variants (L1879-1949)
- Pattern-specific structs: `PatIdent` (L1953-1964), `PatStruct` (L2023-2034)

### Item Definitions (L1393-1681)
- `Item` enum covering all top-level Rust constructs (L1398-1464)
- Individual item implementations: `ItemFn` (L1510-1520), `ItemStruct` (L1595-1607)

### Macro Handling (L1785-1814)
- `Macro` struct with special `TokenStreamHelper` usage (L1790-1793)
- `MacroDelimiter` enum for delimiter variants (L1797-1814)

## Special Handling Patterns

### Token Stream Hashing
Uses `TokenStreamHelper` wrapper for consistent TokenStream hashing (imported L5):
- `Expr::Verbatim` (L526)
- `ForeignItem::Verbatim` (L1166) 
- `Item::Verbatim` (L1461)

### Empty Match Arms
Some enums have uninhabited variants with empty match arms:
- `ImplRestriction` (L1383-1390): `match *self {}`
- Zero-field structs like `TypeInfer` (L2545-2550)

### Feature Flag Dependencies
- "derive" feature: Core derive macro support types
- "full" feature: Complete language construct support  
- "extra-traits" feature: Additional trait implementations (all impls gated on this)

## Dependencies
- `std::hash::{Hash, Hasher}` (L6)
- `crate::tt::TokenStreamHelper` for token stream hashing (L5)

## Critical Invariants
1. All enum variants must have unique u8 discriminants for hash consistency
2. Struct field hashing order must match field declaration order
3. TokenStream types require special wrapper handling
4. Feature flag consistency across related types