# examples/.cargo/registry/src/index.crates.io-6f17d22bba15001f/syn-2.0.110/src/gen/eq.rs
@source-hash: d7428672d82c1931
@generated: 2026-02-09T18:06:37Z

This file is an auto-generated implementation of equality traits for all AST nodes in the `syn` crate, providing structural equality comparison for Rust syntax tree elements.

## Primary Purpose
Auto-generated implementation of `Eq` and `PartialEq` traits for all syntax tree node types in the syn crate, enabling deep structural comparison of parsed Rust code elements. Generated by syn-internal-codegen and should not be manually edited.

## Key Dependencies
- `crate::tt::TokenStreamHelper` (L5): Helper for comparing TokenStream values with equality semantics
- Feature flags control availability: `derive`, `full`, `extra-traits`

## Architecture Pattern
Follows a consistent implementation pattern across all types:
1. Empty `Eq` trait implementations (marker trait)
2. `PartialEq` implementations with field-by-field comparison
3. Enum variants use exhaustive pattern matching with `_ => false` fallback
4. Token streams compared via `TokenStreamHelper` wrapper
5. All implementations gated by `#[cfg(feature = "extra-traits")]`

## Core Type Categories

### Expression Types (L262-760)
- `Expr` (L262): Master expression enum with 40+ variants covering all Rust expression types
- Individual expression types like `ExprBinary` (L395), `ExprCall` (L427), `ExprPath` (L617)
- Feature-gated variants: many expressions only available with `#[cfg(feature = "full")]`

### Type System (L1919-2136)  
- `Type` (L1919): Master type enum covering arrays, functions, paths, references, etc.
- Specific types like `TypePath` (L2077), `TypeReference` (L2098), `TypeArray` (L1954)
- Generic type parameters and bounds: `TypeParam` (L2026), `TypeParamBound` (L2038)

### Item Declarations (L1099-1306)
- `Item` (L1099): Top-level items (functions, structs, enums, modules, etc.)
- Specific items: `ItemFn` (L1169), `ItemStruct` (L1239), `ItemEnum` (L1147)
- Implementation blocks: `ImplItem` (L1016), `ImplItemFn` (L1054)

### Pattern Matching (L1469-1617)
- `Pat` (L1469): Pattern types for destructuring and matching
- Specific patterns: `PatIdent` (L1504), `PatStruct` (L1568), `PatTuple` (L1579)

### Attributes & Metadata (L61-82)
- `Attribute` (L75), `AttrStyle` (L61), `Meta` (L1421)
- Macro-related: `Macro` (L1393), `MacroDelimiter` (L1404)

### Literals (L1329-1370)
- `Lit` (L1329): All literal types with special `Verbatim` handling using string comparison
- Specific literals: `LitStr` (L1370), `LitInt` (L1368), `LitBool` (L1350)

## Key Implementation Details

### TokenStream Handling
Uses `TokenStreamHelper` wrapper for comparing `TokenStream` values in `Verbatim` variants, ensuring proper equality semantics for unparsed token sequences.

### Feature Flag Organization
- `derive` feature: Basic derive macro support
- `full` feature: Complete language support (statements, items, expressions)
- `extra-traits` feature: Gates all equality implementations in this file

### Enum Comparison Strategy
Exhaustive pattern matching ensures only identical variants compare equal, with explicit `_ => false` fallback for type safety.

## Critical Invariants
- All implementations are structurally recursive - equality holds if and only if all constituent parts are equal
- TokenStream comparison uses semantic equality via `TokenStreamHelper`, not pointer equality  
- Feature-gated code paths ensure implementations are only available when corresponding AST nodes exist
- Generated code maintains consistency across all 100+ syntax tree node types